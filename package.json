{
  "name": "gruapp-monorepo",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "start": "npm run start --prefix backend"
  },
  "dependencies": {
    "react-native-leaflet": "^0.0.0-reserved",
    "react-native-webview": "^13.16.0"
  }
}
